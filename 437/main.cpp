#include <iostream>

class Solution{
public:
    int nums = 0; // the num of paths that the
    int current = 0; // current sum of this path
    int function(TreeNode* root, int targetSum){
        if(root == NULL) return 0;
        if(root->left == NULL && root->right == NULL){

        }
    }
};

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}
